/*
执行顺序：FROM>ON>JOIN>WHERE>GROUP BY>WITH CUBE or WITH ROLLUP>HAVING>SELECT>DISTINCT>ORDER BY>TOP

mysql_num_fields() 返回结果集的字段数（列数）
mysql_num_rows()返回结果集的条目数（行数）

f(条件，表达式1，表达式2)
如果条件为真（即不为0），执行表达式1，否则，执行表达式2.

外关键字(或外码)的定义是：如果一个关系中的属性或属性组合并非该关系的码，但却是另外一个关系的主码，则称其为该关系的外码。

 */

public class Test {

}
